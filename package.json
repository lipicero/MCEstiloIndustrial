{
  "name": "mcestilo-industrial",
  "version": "2.0.0",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--no-deprecation concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3"
  }
}
